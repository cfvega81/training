//Tipo de dato enum
enum Estado { //Es una restricción de valores del mismo tipo
    Activo = 1,
    Inactivo = 0,
}

enum BladeStatus {
    Up = 'arriba',
    Middle = 'medio',
    Down = 'abajo',
}